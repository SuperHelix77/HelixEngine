# Release cohort recheck and next offline falsifier

## Qualification correction

OBSERVED from the retained artifacts: the apparent Astra High maintenance
78.38% input / 75.93% output result in `astra-maintenance-edits-v1-20260910`
does not satisfy the ordinary-final contract. Its only candidate answer,
`on-run/turn-1-answer.txt`, is a 687-byte JSON edit list. Its SHA-256 is
`b268fa6575b5b7a401292edd4c2c2d0d1907b3e6760138457edb05b8c9ad6cea`.
Control returned ordinary prose. The pair cannot be promoted as normal-answer
parity or used as a release median cell.

The original runner stopped on an unexpected task-file modification. The later
`reconciled-audit.json` admits an exact zero-byte caller-owned publication lock
through a narrow offline amendment. It explicitly retains
`clean_frozen_protocol_pass=false`, `normal_codex_app_integration=false` and
`general_release=false`. That reconciliation does not change the JSON final
answer into an ordinary model-written report. Preserve both the original error
and the qualified reconciliation; do not describe the lock as a semantic defect.

A read-only historical handoff described this as an observed recovered N=1
maintenance result. Root inspection rejected its inclusion in the normal-final
map. The handoff also recommended filling Sol selection because it was missing.
A missing cell alone is not a falsifiable optimization mechanism, so that is
not authorization for an immediate native benchmark.

## Current installed Astra cost boundary

The newer installed maintenance pair remains the applicable negative evidence:
106,458 / 106,628 input and 1,453 / 1,782 output, control / enabled. Both had five
model segments and passed finite behavioral checks with normal finals.
[Full report](ASTRA_INSTALLED_MAINTENANCE_20260912.md).

CONDITIONAL, holding all other observed candidate segments fixed:

- The 65% output target allows 508.55 output tokens.
- Removing the entire 1,119-token extra-probe segment would still leave 663,
  or only 54.37% output savings. This is deliberately an over-generous bound:
  that segment contains useful semantic tests that cannot simply be deleted.
- Removing both discovery segments as well would leave 457 for patching and
  the final answer, with only 51.55 tokens left for replacement probe work and
  any new interface. A small test helper alone is therefore not a credible
  route to the target on this frozen trajectory.

The visible probes include literal-operator interpretation, Unicode, repeated
terms, invalid types, project isolation, exact evidence, index tampering and
missing source objects. Do not classify their entire program as mechanical
setup. A helper can remove imports, temp-directory handling or repeated
exception scaffolding; it must preserve the model's chosen assertions and its
ability to invent new ones.

## Better bounded lead: Sol dependency scheduling

The retained `sol-varied-coding-v1-20260910/audit.json` records the following
source-only dependency-scheduling pair. These are historical development
counters, not fresh installed-path qualification:

| Measure | Control | Source-only candidate |
|---|---:|---:|
| Input | 120,796 | 16,640 |
| Output | 2,073 | 512 |
| Model segments | 6 | 1 |
| Raw tool calls | 5 | 0 |

Both passed five public tests plus a finite 353 valid/cycle and nine invalid
case evaluation. The task requires lexicographically smallest topological
ordering, referenced nodes, duplicate edges, type validation, cycle rejection,
and input nonmutation. The candidate's final source is excluded from the
ordinary-final release contract.

CONDITIONAL: against that historical control, retaining the source-generation
cost would leave **25,638.6 input and 213.55 output tokens** for native tool
invocation, checked execution, semantic reconsideration and a normal final
before crossing below 65/65. This is a more concrete headroom test than starting
an unknown selection cell. It is not a forecast, an independent replication,
or permission to reuse the old control in a fresh qualification pair.

## One next offline design

Test whether the source-only mechanism can be expressed through ordinary
internal native editing/testing tools, followed by a model-written final:

1. Prepare the same exact task evidence before the first semantic turn, charging
   preparation and preserving source/provenance boundaries.
2. The model owns the code change and semantic probes. Express implementation
   as internal tool actions, never as a caller-rendered substitute final.
3. Engine/native tools execute authorized mechanics, retain exact diagnostics,
   and return failures and unresolved obligations.
4. The model receives the result and writes its ordinary final answer, with
   tools and further semantic investigation still available.

Before any new hosted pair, demonstrate actual installed-path delivery and
execution with a deterministic local provider. No research-only caller service
may silently stand in for an absent installed feature. Prove that source bytes
are current, protected files remain protected, edits execute once, test failure
reaches the model, and incomplete/ambiguous outcomes do not become success.
Do not promote archived source or instructions into hook authority.

Reject the offline candidate if it needs an extra model call merely to learn
Engine, lacks an actual installed integration point, substitutes a final, or
cannot preserve failure/recovery. If it survives, preregister one fresh paired
Sol High dependency task with both complete native counters and normal finals.
Charge preparation, review, retries and Engine cost. Preserve the seven fixed
cells and all negative evidence; no model-wide median is qualified today.

## Provenance and scope

Retained Astra results SHA-256: `98eb88ffddce6f470bc2e18bd62b6a8a52c88f6a08943ca603b2c6fec0f94b42`.

Retained Sol audit SHA-256: `eaf47978afc2832ce6f98a3069d6a413f14baabdd628878a455cd5fc1f02ca8b`.

This recheck inspected stored artifacts and visible tool programs; it did not
inspect hidden reasoning or invoke a new benchmark model. The exploration
subagent and root review did consume research inference. Full research savings,
including the 90% cheaper-testing objective, remain unproven.
