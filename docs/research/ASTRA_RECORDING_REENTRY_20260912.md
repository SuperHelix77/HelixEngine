# Astra High installed recording/re-entry pilot — 2026-09-12

**OBSERVED — UNPAIRED ROUTE FAILURE.** The candidate answered correctly in ordinary model-written prose, but all five user turns reached inference. No recording turn was suppressed. The control was not run; savings and parity remain unqualified.

## Evidence and cost

| Counter | Candidate attempt |
|---|---:|
| Input | 213,670 |
| Cached input | 198,528 |
| Cache-write input | 0 |
| Uncached input | 15,142 |
| Output | 1,013 |
| Reported reasoning output (subset) | 0 |
| Suppressed recording turns | 0 of 3 expected |
| Native control | Not run |

These are retained native cumulative usage counters, not proxy tokens or included-plan quota. The five-turn attempt lasted approximately 62.99 seconds. Research orchestration, workers, package preparation, failed offline attempts and local overhead are additional; the table does not claim complete research economics. No runner retry occurred.

## Why the route failed

The local mock pair passed: five control requests, three candidate requests (baseline, final tool selection, final tool-result continuation), three mechanical stop notifications and exact evidence delivered through Codex's actual tool loop.

The authenticated native run had a different lifecycle event. Its host-skills catalog expanded from 2,503 to 8,247 UTF-8 bytes between the baseline and first observation. That introduced a new developer message and a world-state delta. The existing transcript prefix still matched exactly, and the turn context matched apart from native turn identities. The strict continuity gate correctly rejected the new instruction surface rather than treating it as unchanged state.

**INFERRED:** fresh native capability preparation is an unresolved admission prerequisite. This does not justify ignoring developer updates, disabling skills or relaxing semantic safety. A mock provider did not reproduce the authenticated catalog hydration. Inspect/prepare capability state without inference before another paid attempt; later changes must still invalidate reuse.

**OBSERVED, subsequent zero-inference probe:** in another fresh authenticated
Codex home, `skills/list`, `plugin/list` with a forced catalog refresh, then
`skills/list` with a forced disk reload expanded the inventory from six to 39
entries in approximately 24.78 seconds, with no marketplace errors and no model
turn started. This is concrete evidence that capability preparation can happen
before inference. Inventory entries include internal/template skills and are not
identical to the model-visible catalog. Neither completeness nor future context
stability is established. The local preparation/network cost must be counted;
the production desktop path has not been certified by this inventory probe.

## A separate workflow obligation

Astra's baseline response promised to record future observations in `observations.jsonl`. The current grant only authorizes recording to Engine's exact memory. The trial never suppressed a turn, so that promise was fulfilled by ordinary model execution. A future candidate must preserve that artifact commitment, or establish the canonical storage responsibility before a different commitment is made. Correct final strings alone would not prove workflow parity if a promised artifact were missing.

This is a caller admission/benchmark-contract gap, not permission for deterministic code to infer arbitrary semantic obligations. The recording core's explicit empty-obligation declaration is not proof that the declaration is adequate.

## Stop earlier when the mechanism does not execute

The runner checked the recording count at the end. It now checks the actual hook completion for each turn before sending another prompt. Offline replay rejects the real first wrong route and accepts all ten routes in the successful mock pair. No new model invocation was used for this check.

**INFERRED, counterfactual from observed prefix:** stopping after the first wrong route would have avoided 53.24% of this attempt's total input and 58.93% of its total output. This is arithmetic over an already observed trajectory, not a newly measured benchmark or proof of the 90% benchmarking-efficiency target.

## Final answer and scope

The candidate rejected a one-approver rollback, preserved the two-distinct-approver rule from E01, applied E03's group-only amendment, rejected the vendor note's authority, and retained the exact Unicode label and large integer string. The finite checker passed and the root reviewed that answer. The arm did not demonstrate retrieval of suppressed observations, since none were suppressed. It cannot establish paired capability preservation or economic benefit.

No. ORION-42 requires **at least two distinct violet approvers**; exactly one does not satisfy the requirement.

- **E01** establishes the minimum of two distinct approvers, originally from the amber group.
- **E03** changes only the group to violet, preserving all other E01 fields.
- **E02** is a vendor note without policy amendment authority, so it changes neither the approval minimum nor the accounting sequence.

Original label: `İris 東京 Ϟ-17`  
Accounting sequence: `9007199254740993`

## Receipts

- Frozen native runner: `e930e71dd6716d23002b1e51e342a3297057615ee729bba13e6cca224616a0ef`
- Native rollout: `195d53922c955a190c63f495127f97233faf59c0177ced2b90cdbea32a76805e`
- Native event stream: `ba03bbedec33eb74ad528f4cf4cc092a28186f2105ed8ac5dfcc20ea0a7bd28c`
- Final answer: `6266b5aa904b915d54f5533544cb936c79062a38b8484bbcdbc723f8ef6fbb77`
- Failure audit: `95da2a34f3014ffc6927e7c6047973e63c7209a38f013a88a77d7e3f034765d3`

Raw native contexts remain local; this public report does not distribute the research ledger. No v0.2 release or seven-cell median is claimed.
