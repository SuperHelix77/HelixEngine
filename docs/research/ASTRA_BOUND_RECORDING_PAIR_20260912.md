# Astra High: installed recording and exact artifact pair

## Verdict

OBSERVED: the corrected installed path suppressed all three recording turns,
preserved every requested artifact byte, and returned an ordinary model-written
semantic answer. The root reviewed both final answers: each correctly rejects
one violet approver, applies E01 plus E03's group-only amendment, rejects E02's
vendor override, and preserves the exact label and numeric string.

This is one known five-turn development task, not a holdout, W50 result,
model-wide intelligence-parity proof, seven-cell median, or release qualification.
The **65% output requirement is not met**. No model is promoted.

| Native receipt measure | Control | Helix | Saved |
|---|---:|---:|---:|
| Total input | 159,228 | 54,403 | 65.83% |
| Cached input | 145,536 | 46,080 | — |
| Cache-write input | 0 | 0 | — |
| Uncached input | 13,692 | 8,323 | 39.21% |
| Output, including reported reasoning | 913 | 454 | 50.27% |
| Reported reasoning subset | 27 | 65 | — |
| Distinct native usage updates | 9 | 3 | — |
| Model-issued commands | 4 | 1 | — |
| Suppressed observation turns | 0 | 3 | — |
| Final-answer UTF-8 bytes | 544 | 624 | — |
| Arm wall time, including preflight | 67.396 s | 43.690 s | — |

Reported reasoning is a subset of output, not an additional charge. No hidden
reasoning is inspected. The longer candidate final demonstrates that the measured
output win did not require shortening or replacing the user-facing answer.
Cached-sensitive input savings are not monetary or included-plan quota savings.

## What changed and what actually ran

Both arms received the same explicit existing-file/exact-append contract, same
Astra High effort and ordinary tools. Both used the installed UserPromptSubmit
capture adapter; control had no suppression grant. The caller created an empty
`observations.jsonl` in both workspaces. Candidate recording used an explicit
exclusive-write grant and the installed exact materializer from e4a56e3.

Caller capability preflight ran before any model turn: 19.639 s control and
18.371 s candidate. Later context changes would still invalidate the grant;
none invalidated these three recording transitions. This is tested caller
preparation, not proof of automatic desktop-wide capability readiness.

Control generated three Python append procedures and later read the file with
`cat`. Candidate delegated those appends to Engine, then used the provided exact
Memory replay command during its final semantic turn. It did not read Engine
source into model context. Both final artifact SHA-256 values equal
`46cadd07b9e2ecbdb5c91ee17db89d60d62b3d76755cca225c043bcb7c258551`.
All three intermediate snapshots and the final snapshot passed byte comparison.

The remaining observable candidate mechanics include copying the relatively
long exact-replay command. The control used a short file read. This identifies
an interface cost to analyze offline; it does not establish that removing its
characters will proportionately remove native reasoning or output tokens.

OBSERVED: candidate segment outputs were 49, 202 and 203 tokens. Its 202-token
tool-selection segment used the advertised replay command; the control's file
read segment used 51 tokens. Holding the other candidate segments fixed and
substituting 51 for 202 would produce 303 total output tokens, or 66.81% savings.
This is conditional replay arithmetic, **not a measured candidate** or prediction
that Astra will behave identically. It gives a specific next falsifier: verify
the completed artifact's current binding before offering its simpler retrieval
path, with exact Memory fallback on stale or unavailable files. Do not substitute
an unchecked mutable file for authenticated historical evidence.

## Qualification and costs

Preregistered order was candidate then control (seed 20260912), with isolated
homes, sessions, memory and workspaces. Runtime/fixture/checker bindings were
frozen after the credential-free offline gate. Exact checker mutation tests,
real native-tool replay and serialized delivery checks passed before inference.
The runner stopped on any per-turn route/artifact mismatch and made no retry.
No child was observed in either native arm. Source inspection of the command
trace found only the described recording/recovery work, with no evaluator or
future-fixture reads. The root's semantic review is finite, not independent
capability replication.

The earlier unpaired route failure remains retained separately with its full
213,670 input / 1,013 output cost; no control or saving is invented for it. This
new task explicitly binds a file promise the old task left underspecified, so
its numbers do not revise the old experiment. Preparation, workers, package
verification, failed attempts and root adjudication are additional research
costs. Full research economics and 90% cheaper benchmarking remain unproven.
Local file/store metrics are partial application I/O, not measured physical
storage traffic. A new hosted pair was spent only after offline gates passed.

## Receipt provenance

Installed wheel SHA-256:
`ea8d857238b50939abc099ec97688c6e4a55ff69a72233b86a1b3c494fe73ca6`.
Native counters reconcile to the last recorded thread usage totals; final text
reconciles to completed native final-answer events. Exact event/rollout hashes
and the preregistration/runner remain in the local research evidence directory.
The capsule carries the native-rollout and final hashes. Raw private native
traces and the research ledger are not bundled in the release.

## Control final answer

Rendered below; the capsule retains the hash of the original native bytes.

No. ORION-42 requires **at least two distinct violet approvers**, so exactly one is insufficient.

- **E01** (epoch 1, sequence 1) establishes the minimum of two distinct approvers.
- **E03** (epoch 1, sequence 3) changes only the approver group from amber to violet; all other E01 fields remain unchanged.
- **E02** (epoch 1, sequence 2) is a vendor note without policy amendment authority, so it changes neither the approval minimum nor the accounting sequence.

Original label: `İris 東京 Ϟ-17`<br>
Accounting sequence: `9007199254740993`

## Helix final answer

No. ORION-42 rollback requires **at least two distinct violet approvers**; exactly one is insufficient.

- **E01 (epoch 1, sequence 1)** establishes a minimum of two distinct approvers, originally from the amber group.
- **E03 (epoch 1, sequence 3)** changes only the approver group to violet and explicitly preserves all other E01 fields.
- **E02 (epoch 1, sequence 2)** is a vendor note without policy amendment authority. Its proposed single-approver requirement and replacement accounting sequence do not override E01.

The original values remain:
- Label: `İris 東京 Ϟ-17`
- Accounting sequence: `9007199254740993`
